# Security Policy

Ghost는 회의 오디오·전사라는 민감한 데이터를 다루는 로컬 우선(local-first) 앱입니다. 보안 문제를 진지하게 받습니다.

## 지원 버전

| 버전 | 지원 |
|------|------|
| 최신 릴리즈 | ✅ |
| 그 외 | ❌ (최신으로 업데이트해 주세요) |

## 취약점 제보

**공개 이슈로 올리지 마세요.** 다음 경로로 비공개 제보를 부탁드립니다:

- GitHub [Private vulnerability reporting](https://github.com/HarryKane11/ghost/security/advisories/new) (권장)

영업일 기준 7일 안에 응답을 목표로 합니다.

## 설계상 보안 경계

알고 계시면 좋은 현재 구조:

- **오디오는 기기를 떠나지 않습니다** — 단, 클라우드 전사(ElevenLabs)를 *직접 선택*한 경우 오디오가 해당 API로 전송됩니다.
- **로컬 API 인증** — 데스크탑 앱이 기동 시 세션 토큰을 발급해 백엔드와 렌더러에 주입하며, 패키징 앱의 백엔드는 토큰 없는 요청을 거부합니다. CORS는 localhost로 제한됩니다.
- **API 키 저장** — `~/.ghost/.env`에 0600 권한으로 저장됩니다. OS 키체인 연동은 로드맵에 있습니다.
- **회의 데이터** — `~/Ghost/meetings/` 평문 저장(로컬 우선 설계). 디스크 암호화(FileVault) 사용을 권장합니다.
